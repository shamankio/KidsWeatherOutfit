package com.rustanovych.kidsoutfit.domain.model

/** Which child illustration(s) to show on the home screen. */
enum class ChildGender {
    BOY,
    GIRL,

    /** Show boy and girl outfits side by side. */
    BOTH,
}
